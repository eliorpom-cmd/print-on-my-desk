# The agent's only dependency, and it has it because there is no other way to
# speak to a USB printer from Python. Everything else - HTTP, JSON, base64 -
# is the standard library, on purpose: this runs unattended on a box in a flat,
# and every package added is a package to keep up to date there.
pyusb>=1.2

# For PRINTER = "ble" only, and not installed by default.
#
# bleak is the cross-platform Bluetooth Low Energy library: CoreBluetooth on
# macOS, BlueZ on Linux, WinRT on Windows. It is a real dependency tree, and a
# USB printer has no business pulling it in - which is why ble_printer.py
# imports it inside a try/except and says so plainly if it is missing.
#
#   pip install bleak
#
# bleak>=0.22
